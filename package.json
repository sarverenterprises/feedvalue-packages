{
  "name": "feedvalue-packages",
  "private": true,
  "type": "module",
  "description": "Official FeedValue SDK packages for React, Vue, and vanilla JavaScript",
  "repository": {
    "type": "git",
    "url": "https://github.com/sarverenterprises/feedvalue-packages.git"
  },
  "author": "Sarver Enterprises",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "eslint packages/*/src",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish",
    "release:oidc": "pnpm build && cd packages/core && npm publish --access public --provenance && cd ../react && npm publish --access public --provenance && cd ../vue && npm publish --access public --provenance"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.18.0"
  }
}
